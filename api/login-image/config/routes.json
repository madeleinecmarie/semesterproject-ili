{
  "routes": [
    {
      "method": "GET",
      "path": "/login-images",
      "handler": "login-image.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/login-images/count",
      "handler": "login-image.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/login-images/:id",
      "handler": "login-image.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/login-images",
      "handler": "login-image.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/login-images/:id",
      "handler": "login-image.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/login-images/:id",
      "handler": "login-image.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
